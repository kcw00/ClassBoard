{
  "name": "grade-calculator-lambda",
  "version": "1.0.0",
  "description": "Lambda function for automated grade calculations",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "package": "npm run build && zip -r grade-calculator.zip dist/ node_modules/"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}