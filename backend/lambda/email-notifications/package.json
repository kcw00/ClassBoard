{
  "name": "email-notifications-lambda",
  "version": "1.0.0",
  "description": "Lambda function for sending email notifications",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "package": "npm run build && zip -r email-notifications.zip dist/ node_modules/"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "@aws-sdk/client-ses": "^3.0.0",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}